import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Playground } from '../../../docs/Playground';
import { Pill } from './Pill';
import { Typography } from '../..';

# Pill

This component will render a pill component with nothing inside.

## Props

<ArgsTable of={Pill.Rounded} />

## Usage

Basic usage

```javascript
<Pill.Rounded color={(t) => t.color.cta} label="pill test" onClose={() => {}} />
```

## Playground

<Playground
  code={`<Pill.Rounded color={t => t.color.cta} label=
  "pill test" onClose={() => {}}/>`}
/>

import { createTheme } from './theme';
import type { CreateThemeConfig, Theme } from './theme.types';

const theme = createTheme();

export default theme;
export { createTheme };
export type { CreateThemeConfig, Theme };
